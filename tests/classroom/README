To run this example run, in a shell of two different hosts:

********* Set up:

Remember changing the .connect(Container) in pupil.asl to the 
name of the container where the agent teacher runs.

Copy the corresponding ".asl" files into the folder "agents".


*********HOST 1*****

./ejason -c teacherContainer



Then start agent teacher with:

> ejason:build(teacher).
> ejason:run("teacher").


*********HOSTS N*****

./ejason -c PupilContainer

Then start different pupils  with:

> ejason:build(pupil).
> ejason:run("pupil").







